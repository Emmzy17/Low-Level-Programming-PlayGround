#include <stdio.h> 

int main(){
	int length = 2;
	int breadth = 3;

	int area = length*breadth;
	int perimeter = length+breadth;

	printf("Area of a reactangle of length 2cm and breadth 3cm is %d", area);
	return 0;
}
